_RWS := rec(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a,a^-1,b,b^-1,c,c^-1],
  inverses := [a^-1,a,b^-1,b,c^-1,c],
  equations := [
      [a^2*c^-1*a*c,IdWord],
      [a * c * b^2 * a^-1 * c^-1 * b,IdWord],
      [a^-1 * b^-1 * a * b * c^3,IdWord]
  ]
);
